# Secrets for the ops stack. Copy to ./env, chmod 600, fill in.
# Extract from the local stellar CLI keychain (never commit the filled file):
#   stellar keys secret pb-mm --config-dir .stellar
#   stellar keys secret pb-trader --config-dir .stellar
PB_SECRET_PB_MM=S...
PB_SECRET_PB_TRADER=S...
