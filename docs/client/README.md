# PageBook web client

The published page is
[blob.tomerweller.com/pagebook/client](https://blob.tomerweller.com/pagebook/client/).
The source now lives in [`clients/web/`](../../clients/web/). This directory
kept the old no-build `.mjs` page; that tree is in git history.

The Pages workflow builds `clients/web` and puts the output at
`/pagebook/client/`.
